"""Normally this would encrypt the password but can't show it for obvious reasons"""

pwd_context = None  # Placeholder for password context

def hash_pwd(password: str) -> str:
    """Hash the password"""

    return password

